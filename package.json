{
  "name": "festmap-fe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "start": "npx serve . -l 3000",
    "vercel-build": "mkdir -p public && cp -R index.html privacy.html privacy ic_festmap.svg public/ 2>/dev/null || true && echo \"No build needed\"",
    "build": "npm run vercel-build",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "serve": "^14.2.0",
    "vercel": "^51.8.0"
  }
}
